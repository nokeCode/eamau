import '../../models/admission/admission_request_model.dart';

class AdmissionRemoteDatasource {
  Future<bool> submitAdmission(AdmissionRequestModel request, List<String> documentPaths) async {
    // upload and submit to API
    return false;
  }
}
